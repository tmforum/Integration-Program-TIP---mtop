<?xml version="1.0" encoding="UTF-8"?>

<pluginMetadata>
<mojos>
<mojo>
<call>package-ddp-target</call>
<goal>package-ddp</goal>
<phase>package</phase>
<description><![CDATA[
Preform the ant tasks required to package a mTOP DDP
(zip up the contents of the DDP directories)
]]></description>
<parameters>
    <!-- parameter>
        <name>basedir</name>
        <defaultValue>${basedir}</defaultValue>
        <required>true</required>
    </parameter -->
    <parameter>
        <name>package-name</name>
        <defaultValue>${project.artifactId}</defaultValue>
        <required>true</required>
    </parameter>
    <parameter>
        <name>package-description</name>
        <defaultValue>${project.description}</defaultValue>
        <required>true</required>
    </parameter>
</parameters>
</mojo>
</mojos>
</pluginMetadata>
