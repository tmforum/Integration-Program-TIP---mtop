<?xml version="1.0" encoding="UTF-8"?>

<project>
    <target name="package-ddp-target">
        <echo>${package-name}.zip is a Packaging of the ${package-description}</echo>
        <mkdir dir="${basedir}/target" />
        <zip destfile="${basedir}/target/${package-name}.zip" 
            basedir="."
            excludes="target, target/*"
            comment="Package of ${package-description}"/>
    </target>
</project>
